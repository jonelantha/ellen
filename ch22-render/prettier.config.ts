import type { Config } from 'prettier';

const config: Config = {
  semi: true,
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  arrowParens: 'avoid',
  trailingComma: 'all',
};

export default config;
